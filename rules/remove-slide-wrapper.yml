id: remove-slide-wrapper
message: Remove #slide[] wrapper to use heading-based slides
language: typst
severity: hint
rule:
  pattern: "#slide[$$$BODY]"
  has:
    kind: heading
    stopBy: end
fix: "$$$BODY"
